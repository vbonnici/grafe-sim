#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using namespace std;
using ll = long long int;

int main() {
  int A1, A2, A3; cin >> A1 >> A2 >> A3;
  A1 + A2 + A3 >= 22 ? cout << "bust" << endl : cout << "win" << endl;
}