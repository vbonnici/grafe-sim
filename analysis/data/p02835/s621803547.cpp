#include <bits/stdc++.h>
using namespace std;
int main(void){
    
    //*
    int a, b, c;
    cin >> a >> b >> c ;
    //*/

    /*
    vector<int> vec(3);
    cin >> vec.at(0) >> vec.at(1) >> vec.at(2) ;

    sort(vec.begin(), vec.end());
    //*/
    
    //*
    if( a + b + c > 21 ){
        cout << "bust" << endl;
    } else {
        cout << "win" << endl;
    }
    //*/
    
    //cout << ans << endl;

    return 0; 
}
