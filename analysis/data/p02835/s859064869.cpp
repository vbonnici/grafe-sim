#include <bits/stdc++.h>
using namespace std;
double pi = 2 * acos(0.0);
int main(){
        int a, b, c; cin >> a >> b >> c;
        if (a+b+c >= 22) cout << "bust";
        else cout << "win";
    return 0;
}

