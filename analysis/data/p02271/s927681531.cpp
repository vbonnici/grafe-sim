#include<iostream>
using namespace std;

bool isMade( int *arr, unsigned int arr_size, unsigned int from_i, int key ){

	if( key==0 ){
		return true;
	}else if( from_i >= arr_size ){//?????¶????????????????????§???! ?????????????´?????????£?????????????????°????????????????????????????????????
		return false;
	}else{
		return isMade( arr, arr_size, from_i+1, key)/*arr[from_i]????????°????????´???*/ || isMade( arr, arr_size, from_i+1, key -arr[from_i] );//arr[from_i]????????¶??´???(key??????????????£?????°????????????????????????????????????)
	}
}

																																		

int main()
{
	int sSrc, *src, sKeys;
	cin>>sSrc;
	src=new int[sSrc];
	for( int i=0; i<sSrc; i++ ){
		cin>>src[i];
	}

	cin>>sKeys;
	for( int i=0, key; i<sKeys; i++ ){
		cin>>key;
		cout<<( isMade( src, sSrc, 0, key ) ? "yes" : "no" )<<endl;
	}
}